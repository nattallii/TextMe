apiVersion: v1
kind: Service
metadata:
  name: {{ include "mongo-chart.fullname" . }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: 27017
      protocol: TCP
      name: mongo
  selector:
    app: {{ include "mongo-chart.fullname" . }}