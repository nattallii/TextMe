[project]
name = "textme"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "pyjwt>=2.11.0",
]

[tool.uv.workspace]
members = [
    "chat-service",
    "auth-service",
]
